# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt
# Project dependencies for the AI-Powered Test Case Lifecycle Platform backend.
# Pin versions before production deployment.
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0          # PostgreSQL async driver
psycopg2-binary>=2.9.9   # PostgreSQL sync driver (migrations)

# Caching & task queue
redis>=5.0.0
celery>=5.3.6
flower>=2.0.1            # Celery monitoring UI

# AI & LLM
langchain>=0.1.0
langchain-google-genai>=0.0.9
langchain-community>=0.0.29
chromadb>=0.4.0           # Vector store for RAG
tiktoken>=0.5.2

# Validation & serialization
pydantic>=2.6.0
pydantic-settings>=2.2.0
email-validator>=2.1.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# HTTP clients (external integrations)
httpx>=0.27.0
aiohttp>=3.9.0

# Utilities
python-dotenv>=1.0.1
structlog>=24.1.0
orjson>=3.9.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0            # test client
factory-boy>=3.3.0
